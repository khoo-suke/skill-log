/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'nrorkenuurnazzmosfcq.supabase.co',
      },
    ],
  },
};

export default nextConfig;
